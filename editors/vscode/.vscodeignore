# NOTE: node_modules must NOT be excluded — vscode-languageclient is a runtime
# dependency and the extension is not bundled; vsce prunes devDependencies on
# its own.
.vscode/**
src/**
tsconfig.json
.gitignore
**/*.map
**/*.ts
